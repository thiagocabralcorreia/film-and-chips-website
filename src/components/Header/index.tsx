import { TopBar, LinkTitle } from './styles';

export const Header = () => {
  return (
    <TopBar>
      <LinkTitle to={'/'}>
          F<span>I</span>LM & CH<span>I</span>PS
      </LinkTitle>
    </TopBar>
  );
};
